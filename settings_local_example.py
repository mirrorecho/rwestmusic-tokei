# SAVE A COPY OF THIS AS "settings_local.py" within each repo

# this should be the path that contains the calliope repo (and project repor if project path not specified below)
ROOT_PATH = "/home/randall/Code/mirrorecho" 

# set this to override the default PROJECT_PATH on a per-machine basis
PROJECT_PATH = None 